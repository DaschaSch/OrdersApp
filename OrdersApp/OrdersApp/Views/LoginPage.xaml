<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="OrdersApp"
             x:Class="OrdersApp.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="StartAndExpand">
            <Label Text="Username" />
            <Entry x:Name="usernameEntry" Placeholder="Введите имя пользователя" />
            <Label Text="Password" />
            <Entry x:Name="passwordEntry" IsPassword="true" Placeholder="Введите пароль" />
            <Button Text="Вход" Clicked="OnLoginButtonClicked" />
            <Label x:Name="messageLabel" TextColor="Red" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>